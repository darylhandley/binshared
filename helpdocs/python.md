# create a new environment 
python3 -m venv <dir_name> 

# load it 
source <dir_name>/bin/activate

# install a new package in requirements.txt 
pip install <your_package> 
pip freeze > requirements.txt
